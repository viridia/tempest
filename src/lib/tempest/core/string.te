/** An iterable sequence of characters. The interal representation is a UTF-8 encoded byte sequence.
    For efficient random access to characters, conver the `String` into a character array.
*/
export final class String {
  const size: int = 0;
}
